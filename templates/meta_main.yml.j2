---
# Generated file. Do not edit directly.
# Source template: {{ template_name }}

dependencies: []
galaxy_info:
  role_name: postfix
  namespace: guidugli
  author: guidugli
  company: Creuzo Tech
  description: >-
    Install and configure Postfix as a secure local relay client, including relayhost,
    SASL authentication, TLS settings, aliases, and Molecule coverage across supported Linux containers.
  license: MIT
  min_ansible_version: '2.14'
  platforms:
{% for platform in platforms %}
    - name: {{ platform.name }}
      versions:
{%   for version in platform.versions %}
        - {{ version | tojson }}
{%   endfor %}
{% endfor %}
  galaxy_tags:
    - postfix
    - smtp
    - mail
    - relay
    - sasl
    - tls
    - linux
